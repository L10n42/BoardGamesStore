@model BoardGamesStore.Models.BoardGame

@{
    ViewData["Title"] = "Details";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-7">
            <div id="carouselImages" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner" style="border-radius: 12px">
                    @if (Model.BoardGameImages?.Count > 0)
                    {
                        var firstImage = true;
                        foreach (var image in Model.BoardGameImages)
                        {
                            <div class="carousel-item @(firstImage ? "active" : "")">
                                <img src="@image.ImageUrl" class="d-block w-100" alt="@Model.Name" />
                            </div>
                            firstImage = false;
                        }
                    }
                </div>
                @if (Model.BoardGameImages?.Count > 1)
                {
                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselImages" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselImages" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                }
            </div>
        </div>

        <div class="col-md-5">
            <h1>@Model.Name</h1>
            <h4 class="text-muted">@Model.Category?.CategoryName</h4>

            <p class="card-price">@Model.Price.ToString("0.00$")</p>
            <p>
                <span class="badge @(Model.StockQuantity > 0 ? "bg-success" : "bg-danger")">
                    @(Model.StockQuantity > 0 ? "In Stock" : "Out of Stock")
                </span>
            </p>

            <div class="d-grid gap-2">
                <button class="btn btn-primary btn-lg" @(Model.StockQuantity == 0 ? "disabled" : "")>Купити</button>
                <button class="btn btn-outline-primary btn-lg" @(Model.StockQuantity == 0 ? "disabled" : "")>Додати в кошик</button>
            </div>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-md-12">
            <h3>Description</h3>
            <p>@Model.Description</p>

            <table class="table">
                <tr>
                    <th>Players</th>
                    <td>@Model.NumberOfPlayers</td>
                </tr>
                <tr>
                    <th>Playtime</th>
                    <td>@Model.GameTimeMinutes</td>
                </tr>
                <tr>
                    <th>Suggested Age</th>
                    <td>@Model.SuggestedAge</td>
                </tr>
            </table>
        </div>
    </div>
</div>

@* <div>
    <a asp-action="Edit" asp-route-id="@Model?.BoardGameID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
 *@